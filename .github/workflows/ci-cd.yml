name: CI/CD Pipeline

on:
  push:
    branches:
      - main

  pull-request:
    branches:
      - main

jobs:
  build:
    run-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: Actions/checkout@v3 
