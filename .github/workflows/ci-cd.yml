name: CI/CD Pipeline

on:
  push:
    branches:
      - main

  pull-request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: Actions/checkout@v3 
